<%= form_for category, url: admin_categories_path, data: { turbo: true }, html: { class: 'flex flex-row items-stretch space-x-2' } do |f| %>
  <%= f.text_field :name, class: 'text-form', placeholder: 'Tên loại mới' %>
  <%= f.submit 'Tạo', class: 'btn-lg bg-blue-500 text-white' %>
<% end %>
