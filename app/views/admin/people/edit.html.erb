<div class='p-3'>
  <div class="text-3xl mb-3">Update person information</div>

  <% if flash[:danger].present? %>
    <div class='rounded-md border-red-600 bg-red-300 p-2 text-white'>
      <%= flash[:danger] %>
    </div>
  <% end %>

  <%= render 'admin/people/form', person: @person, url: admin_person_path(@person) %>

</div>
