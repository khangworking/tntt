<div class='absolute top-0 left-0 h-full w-full grid place-items-center'>
  <div class='text-gray-800 font-bold flex flex-col space-y-2 items-center'>
    <div class='lg:text-3xl text-md'><%= t('common.welcome') %></div>
    <div class='text-sm lg:text-lg'>
      <%= t('common.today_is', date: l(Time.zone.now.to_date, format: :long)) %>
    </div>
    <div class='border p-3 border-gray-300 rounded-md'>
      <div class='text-md font-bold mb-3'>
        <% duration = (Person.next_feastday_persons.keys.first - Time.zone.now.to_date).to_i %>
        <% if duration >= 1 %>
          Còn <%= duration %> ngày nữa (ngày <%= l(Person.next_feastday_persons.keys.first, format: :default) %>) là đến bốn mạng của:
        <% else %>
          Hôm nay là bồn mạng của:
        <% end %>
      </div>
      <div class='flex flex-col space-y-2 font-light'>
        <% Person.next_feastday_persons.values.first.each do |person| %>
          <div><%= person.christain_name %> <span class='font-bold'><%= person.fullname %></span></div>
        <% end %>
      </div>
    </div>
  </div>
</div>
