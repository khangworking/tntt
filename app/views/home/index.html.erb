<div class='h-screen w-screen grid place-items-center'>
  <div class='flex flex-col space-y-1'>
    <div class='text-3xl text-gray-800 font-bold'><%= t('common.welcome') %></div>
    <% if user_signed_in? %>
      <%= link_to t('common.actions.logout'), destroy_user_session_path, method: :delete %>
    <% end %>
  </div>
</div>
